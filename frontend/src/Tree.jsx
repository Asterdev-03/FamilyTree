import React from "react";

import Modal from "react-modal";

const rootId = "member-1"; // The root of the tree
const initialData = {
  id: "member-1",
  name: "New Member",
  children: [],
  parents: [],
  siblings: [],
};

const Tree = () => {
  return <div>Tree</div>;
};

export default Tree;
